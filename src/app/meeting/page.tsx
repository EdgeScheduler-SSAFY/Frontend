"use client";
import styled from "styled-components";

export default function Meeting() {
  return <MainLayout>회의 생성 페이지</MainLayout>;
}

const MainLayout = styled.div`
  width: 100%;
  min-height: calc(100% - 50px);
`;
