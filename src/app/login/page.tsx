"use client";
import styled from "styled-components";

export default function Login() {
  return <MainLayout>로그인</MainLayout>;
}

const MainLayout = styled.div`
  width: 100%;
  min-height: calc(100% - 50px);
`;
